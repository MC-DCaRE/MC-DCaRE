## ADDED Requirements

No new capabilities — operational task. All behavior remains unchanged. The system SHALL continue operating as specified by existing specs.

#### Scenario: No-op
- **WHEN** performing calibration runs with updated sequential_times
- **THEN** all existing simulation, calculation, and calibration behavior is preserved
