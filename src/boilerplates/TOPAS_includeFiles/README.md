# Bow-tie STL assets

Processed from `research/Monte Carlo stuff from Inbum/bowtie.stl` by `tools/process_bowtie_stl.py`.

- `fullfan.stl` -- binary STL, recentered, decimated.
- `halffan.stl` -- one lateral half of the full-fan (approximation; TrueBeam half-fan is a distinct filter).

Loaded by TOPAS `TsCAD` (`FileFormat = "stl"`).
